#!/bin/sh

currentalbum="$(mpc -f %album% -q current)"
[ -z "${currentalbum}" ] && exit
mpc | grep -e '^\[playing' && mpc crop || mpc clear
mpc consume on
mpc random off
mpc find album "${currentalbum}" | mpc add
mpc play
musicresume
