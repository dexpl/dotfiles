#!/bin/sh

declare -r en_url='http://www.multitran.ru/c/M.exe?l1=1&l2=2&s='

[ $# -eq 0 ] && {
	echo Too few arguments >&2
	exit 1
}

url=${en_url}$(echo $@ | sed 's/ /+/g')
echo ${url} >&2
links --dump --no-numbering --no-references "${url}" | less -F
