#!/bin/sh

currentalbum="${*:-$(mpc -f %album% -q current)}"
[ -z "${currentalbum}" ] && exit
mpc | grep -qe '^\[playing' && mpc crop || mpc clear
mpc consume on
mpc random off
mpc single off
mpc find album "${currentalbum}" | mpc add
mpc play
musicresume
